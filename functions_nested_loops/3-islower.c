#include "main.h"

/**
 * _islower - Program entry point
 * int c - parameter
 *
 * Return:  0 on success. Error code otherwise
 */

int _islower(int c)
{
	if (c >= 97)
	{
		return (1);
	} else
	{
		return (0);
	}
}
